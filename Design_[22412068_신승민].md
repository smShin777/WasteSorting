#3. Design

<img width="2000" height="2000" alt="570390193-d05e2dd1-a5b7-4248-a58b-f1e3e5150881" src="https://github.com/user-attachments/assets/d54c3a44-8913-40af-b65d-5442f31fcdd4" />

**Student No**: 22412068
**name**: 신승민
**E-mail**: smShin729@gmail.com

## [ Revision history ]
| Revision date | Version | Description | Autor|
| :--- | :--- | :--- | :--- |
| | 1.0.0 | Design 문서 초안 작성 | |

## = Contents =
1. **Introduction**
2. **Class diagram**
3. **Sequence diagram**
4. **State machine diagram**
5. **Implementation requirements**
6. **Glossary**
7. **References**

## 1. Introduction

1) Summary

 우리는 일상생활 속에서 매일 수많은 플라스틱 포장재와 쓰레기를 배출한다. 특히 코로나19 팬데믹 이후 비대면 소비가 일상화되몃서 국내 플라스틱 포장재 폐기물 발생량은 이전 대비 80%나 급증했다. 하지만 우리가 버린 플라스틱이 실제로 다시 자원으로 활용되는 물질 재활용률은 고작 16.4$에 불과한다. 이러한 문제가 발생하는 이유는 많은 시민이 복잡한 환경부의 분리배출 지침을 정확히 알지 못하기 때문이다. 최신 제품의 등장과 강화된 법규로 인해 일반 소비자가 매번 올바른 배출 규칙을 파악하기란 사실상 불가능하다. 이로 인해 시민들은 분리수거를 실천하면서도 의도치 않게 잘못된 방식으로 배출하게 되고, 이는 재활용률 저하뿐만 아니라 과태료 부과라는 불이익으로까지 이어지고 있다. 위의 불편함을 해결하기 위해, 사용자가 쓰레기를 배출하기 전 올바른 요령을 단계별로 확인하고 실천할 수 있도록 돕는 시스템을 고안했다. 사용자는 버리려는 쓰레기의 명칭을 텍스트로 검색하면 최신 배출 지침을 신속하게 획득할 수 있다. 이는 분리배출 4대 원칙 준수를 유도하여 실질 재활용률을 높이는 의의가 있으며, 잘못된 배출로 인한 과태료 부과를 방지하는 유용성을 제공한다. 또한 지침이 변경되거나 신규 품목이 등장해도 관리자가 실시간으로 데이터를 업데이트할 수 있는 구조로 설계하여 확장성을 보장한다.
이 문서는 시스템을 개발하기 위한 세 번째 단계인 Design 명세서이다. 지난 Analysis단꼐의 요구사항을 바탕으로 실제 구현을 위한 세부 사항을 명시하며, Class Diagram, Sequence Diagram, State Machine Diagram 등의 다이어 그램을 통해 시스템의 전반적인 모습을 시각화하고 순서화하여 보여준다. 본 문서를 통해 시스템이 어떤 구조와 흐름으로 구현되는지 전반적으로 보여준다

2) Important poinst of your design
 1. 실시간 텍스트 검색 및 신속한 정보 반환을 통한 유용성 확보
 2. 4대 원칙 준수 유도 및 과태료 방지를 위한 상태 전환 설계

## 2. Class diagram
<img width="899" height="628" alt="image" src="https://github.com/user-attachments/assets/72a77836-e753-4085-9cb8-26c38c2211bb" />

1) Register

| 구분 | 내용 |
| :-- | :-- |
| Attributes | +regId : String : 회원가입 시 입력하는 사용자 ID <br> +regPw : String : 회원가입 시 입력하는 비밀번호 <br> +regEmail : String : 회원가입 시 입력하는 이메일 |
| Methods | +checkDUplicateId(id:String) : boolean : 입력한ID가 이미 존재하는지 중복 확인 <br> +saveUserInfo() : boolean : 사용자 정보를 저장 |

2) Login

| 구분 | 내용 |
| :-- | :-- |
| Attributes | +inputId : String : 로그인 시 입력하는 ID <br> +inputPw : String : 로그인 시 입력하는 비밀번호 <br> +isLoginSuccess : boolean : 로그인 성공 여부 |
| Methods | +requestLogin(id, pw) : boolean : ID와 비밀번호로 로그인 요청 <br> +showLoginError() : boolean : 로그인 실패 시 오류 표시 |

3) DisposalController

| 구분 | 내용 |
| :-- | :-- |
| Attributes | +activeSession : String : 현재 활성화된 세션 정보 |
| Methods | +processRegistration(id, pw, email) : boolean : 회원가입 처리 <br> +ProcessLogin(id, pw) : boolean : 로그인 처리 <br> +processSearchRequest(keyword) : DisposalDetail : 검색 요청 처리 후 결과 반환 |

4) AdminController

| 구분 | 내용 |
| :-- | :-- |
| Attributes | +adminToken : String : 관리자 인증 토큰 |
| Methods | +validateInput(data:RuleData) : boolean : 입력 데이터 유효성 검사 <br> +requestRuleUpdate(itemId, data) : boolean : 규칙 업데이트 요청 |

5) SearchUI

| 구분 | 내용 |
| :-- | :-- |
| Attributes | +searchKeyword : String : 검색창에 입력된 키워드 <br> +currentDisposalInfo : DisposalDetail : 현재 표시 중인 폐기물 정보 |
| Methods | +searchItem(keyword:String) : void : 키워드로 항목 검색 <br> +displayDisposalInfo(info): void : 검색 결과 화면에 표시 |

6) AdminUI

| 구분 | 내용 |
| :-- | :-- |
| Attributes | +inputRuleData : RuleData : 입력된 규칙 데이터 <br> +inputFineData : FineData : 입력된 과태료 데이터 |
| Methods | +clickUpdateBtn() : void : 업데이트 버튼 클릭 이벤트 처리 <br> +showUpdateStatus(result:boolean): void : 업데이트 결과 상태 표시 |

7) Item

| 구분 | 내용 |
| :-- | :-- |
| Attributes | +itemId : int : 항목 고유 ID <br> +itemName : String : 항목 이름 <br> +category : String : 항목 카테고리 |
| Methods | +findItem(keyword:String) : ItemInfo : 키워드로 항목 검색 후 정보 반환 |

8) DisposalRule

| 구분 | 내용 |
| :-- | :-- |
| Attributes | +ruleId : int : 규직 고유 ID <br> +itemId : int : 해당 규칙이 적용되는 항목 ID <br> +instructions : String : 폐기 방법 안내 문자열 |
| Methods | +getDisposalRule(itemId:int) : RuleData : 항목 ID로 폐기 규칙 조회 |

9) FineStandard

| 구분 | 내용 |
| :-- | :-- |
| Attributes | +fineId : int : 과태료 고유 ID <br> +itemId : int : 해당 과태료가 적용되는 항목 ID <br> +fineAmount : int : 과태료 금액 <br> +legalBasis : String : 과태료 부과 법적 근거 |
| Methods | +getFineStandard(itemId:int): FineData : 항목 ID로 과태료 기준 조회 |

10) Admin

| 구분 | 내용 |
| :-- | :-- |
| Attributes | +adminId : String : 관리자 ID <br> +adminPw : String : 관리자 비밀번호 |
| Methods | +checkAdminCredentials(id, pw) : boolean : 관리자ID/비밀번호 인증 확인 |

## 3. Sequence diagram

1) Register

<img width="535" height="459" alt="image" src="https://github.com/user-attachments/assets/b5d2f4ea-eae3-43d0-bec8-4b46475b44db" />
위의 그림은 회원가입 요청 사용자가 ID, PW, Email을 입력하여 processRegistration(id, pw, email)을 호출하고 ID 중복 확인 DisposalController가 Register에 checkDuplicateId(id)를 호출하여 이미 존재하는 ID인지 확인합니다. Register가 중복 여부를 boolean값으로 DisposalController에 반환합니다. 중복이 없다면 saveUserInfo()를 호출하여 사용자 정보를 저장합니다. 혹은 중복이 있다면 저장 없이 ID 중복 오류 메시지를 반환합니다. 만약 저장 결과 반환 중복이 없는 경우 Register가 저장 성공 여부를 true로 반환하고 DisposalController가 회원가입 완료 알림 또는 오류 메시지를 사용자에게 반환합니다.


2) Login

<img width="532" height="454" alt="image" src="https://github.com/user-attachments/assets/ebd9b5ad-0007-4b44-9301-f48bad7b68b3" />
위의 그림은 로그인 요청 사용자가 ID와 PW를 입력하여 processLogin(id, pw)를 호출하고 DisposalController가 Login에 requestLogin(id, pw)를 호출하여 인증을 요청합니다. 인증 결과 반환 Login이 인증 결과를 boolean 값으로 DisposalController에 반환 합니다. 로그인에 성공한다면 activeSession을 생성하고 로그인 성공 알림을 사용자에게 반환합니다. 혹은 로그인에 실패한다면 showLoginError()를 호출하여 오류 메시지를 사용자에게 반환합니다. DisposalController가 로그인 결과를 최종적으로 사용자에게 반환합니다.


3) Search

<img width="896" height="556" alt="image" src="https://github.com/user-attachments/assets/9b26deee-5c92-4e14-885a-a4217dd4a68a" />
위의 그림은 키워드 입력 사용자가 SerachUI에 검색할 키워드를 입력합니다. SearchUI가 DisposalController에 processSearchRequest(keyword)를 호출하여 검색을 요청합니다. DisposalController가 Item에 findItem(keyword)를 호출하여 해당 키워드에 맞는 폐기물 항목을 검색하고 항목 정보 Item이 검색 결과인 ItemInfo를 DisposalController에 반환합니다. 만약 Item이 존재한다면 DisposalController가 DisposalRule에 getDisposalRule(itmeId)를 호출하여 해당 항목의 폐기 규칙을 조회합니다. 혹은 Item이 없다면 이후 과정 없이 결과 없음을 반환합니다. DisposalRule이 해당 항목의 폐기 규칙인 RuleData를 DisposalController에 반환하고 DisposalController가 DisposalDetail을 SearchUI에 반환합니다. SearchUI가 displayDisposalInfo(info)를 호출하여 검색 결과를 사용자 화면에 표시합니다.


4) AdminLogin

<img width="621" height="502" alt="image" src="https://github.com/user-attachments/assets/2ebafd55-7c4f-4bce-95ac-d0baa5c03012" />
위의 그림은 로그인 요청 관리자가 ID와 PW를 입력하여 AdminController에 로그인을 요청합니다. AdminController가 Admin(class)에 checkAdminCredentials(ip, pw)를 호출하여 관리자 인증을 요청하고 인증 결과 반환 Admin(class)가 인증결과를 boolean값으로 AdminController에 반환합니다. 만약 인증에 성공하였다면 adminToken을 발급하고 관리자 페이지 접근을 허용합니다. 혹은 인증에 실패하였다면 오류 메시지를 관리자에게 반환합니다. AdminController가 로그인 결과를 최종적으로 관리자에게 반환합니다.


5) Rule & Fine Update

<img width="886" height="611" alt="image" src="https://github.com/user-attachments/assets/8182f343-4d41-42d7-8672-e3b1e9468f95" />
위의 그림은 데이터 입력 관리자가 AdminUI에 업데이트할 RuleData와 FineData를 입력합니다. AdminUI가 AdminController에 clickUpdateBtn()을 호출하여 업데이트를 요청하고 AdminController가 내부적으로 validateInput(data)를 호출하여 입력된 데이터가 올바른지 검사합니다. 유효성 검사 결과를 boolean값으로 반환합니다. 만약 유효한 데이터일 경우에만 이후 업데이트 과정이 실행합니다. AdminController가 DisposalRule에 requestRuleUpdate(itemId, data)를 호출하여 폐기 규칙을 업데이트하고 DisposalRule이 업데이트 성공 여부를 boolean 값으로 AdminController에 반환합니다. AdminController가 FineStandard에 requestFineUpdate(itemId, data)를 호출하여 과태료 정보를 업데이트하고 FineStandard가 업데이트 성공 여부를 boolean값으로 AdminController에 반환합니다. AdminController가 업데이트 결과를 AdminUI에 반환하고, AdminUI가 showUpdateStatus(result)를 호출하여 관리자에게 결과를 화면에 표시합니다.


## 4. State machine diagram

<img width="884" height="572" alt="image" src="https://github.com/user-attachments/assets/7d6302d5-bc9e-4014-b3b5-7c87aca5b4b7" />
시스템이 시작되면 Client와 Server 모두 Idle 상태에서 대기하며, 사용자의 행동에 따라 상태가 전이되고 사용자는 Idle에서 회원가입, 로그인, 관리자 로그인 중 하나를 선택할 수 있으며, 각각 해당 처리 상태로 전이됩니다. 로그인 성공 시 Client는 Searching, Server는 Session Active 상태로 전이되어 검색 기능을 사용할 수 있습니다. 관리자 인증 성공 시 Client는 Admin Mode, Server는 Admin Session Active 상태로 전이되어 규칙/과태료 업데이트가 가능합니다. 작업 실패 시 현재 상태를 유지하고, 로그아웃 시 Client와 Server 모두 Idle상태로 복귀합니다.

## 5. Implementation requirements

## 6. Glossary

## 7. References
